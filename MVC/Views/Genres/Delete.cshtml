@model MVC.Genre

@{
    ViewData["Title"] = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card text-center">
                <div class="card-body p-5">
                    <div class="text-warning mb-4 display-1">
                        <i class="fas fa-exclamation-triangle"></i>
                    </div>
                    <h2 class="text-gradient mb-4">Delete Genre</h2>
                    <p class="text-muted mb-4">Are you sure you want to delete this genre?</p>
                    <p class="h4 text-gradient mb-4">@Html.DisplayFor(model => model.Name)</p>
                    <div class="d-flex justify-content-center gap-3">
                        <form asp-action="Delete" method="post">
                            <input type="hidden" asp-for="Id" />
                            <button type="submit" class="btn btn-danger">
                                <i class="fas fa-trash-alt me-2"></i> Delete
                            </button>
                        </form>
                        <a asp-action="Index" class="btn btn-secondary">
                            <i class="fas fa-times me-2"></i> Cancel
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>